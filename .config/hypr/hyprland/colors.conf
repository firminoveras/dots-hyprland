# exec = export SLURP_ARGS='-d -c FFDBC9BB -b 6F3C1C44 -s 00000000'

general {
    col.active_border = rgba(FFB4A4dd)
    col.inactive_border = rgba(200F0B00)
}

misc {
    background_color = rgba(200F0BFF)
}

plugin {
    hyprbars {
        # Honestly idk if it works like css, but well, why not
        bar_text_font = Rubik, Geist, AR One Sans, Reddit Sans, Inter, Roboto, Ubuntu, Noto Sans, sans-serif
        bar_height = 30
        bar_padding = 10
        bar_button_padding = 5
        bar_precedence_over_border = true
        bar_part_of_window = true

        bar_color = rgba(200F0BFF)
        col.text = rgba(FFDAD3FF)


        # example buttons (R -> L)
        # hyprbars-button = color, size, on-click
        hyprbars-button = rgb(FFDAD3), 13, 󰖭, hyprctl dispatch killactive
        hyprbars-button = rgb(FFDAD3), 13, 󰖯, hyprctl dispatch fullscreen 1
        hyprbars-button = rgb(FFDAD3), 13, 󰖰, hyprctl dispatch movetoworkspacesilent special
    }
}

windowrulev2 = bordercolor rgba(FFB4A4AA) rgba(FFB4A477),pinned:1
